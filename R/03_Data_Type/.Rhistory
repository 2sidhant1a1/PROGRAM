library(dslabs)
data(heights)
options(digits = 3)
force(heights)
avg_height <- mean(heights$height)
ind <- heights$height > avg_height
sum(ind)
sum(ind & heights$sex == "Female")
mean(heights$sex == "Female")
min_height <- min(heights$height)
min_height
match(min_height)
match(min_height, heights$height)
heights$sex(1032)
heights$sex[1032]
max_height <- max(heights$height)
max_height
x <- <min_height>:<max_height>
x <- 50:83
x
min_height
max_height
x <- 50:82.7
x
sum(!(x %in% heights$height))
heights2 <- heights
heights2$ht_cm <- heights2$height * 2.54
heights2$ht_cm
heights2 <- transform(heights, ht_cm = height * 2.54)
heights2
heights2$ht_cm[18]4
heights2$ht_cm[18]
mean(heights2)
mean(heights2$height)
mean(heights2$ht_cm)
sum(heights2$sex == "Female")
mean(heights2$sex == "Female")
(heights2$sex == "Female"(mean(heights2$ht_cm)))
mean(heights2$ht_cm[heights2$sex == "Female"])
library(dslabs)
data(heights)
x <- ifelse(heights$sex == 'Female', 1, 2)
sum(x)
x <- ifelse(heights$height > 72, heights$height, 0)
mean(x)
inches_to_ft <- function(x){}
inches_to_ft <- function(x){}
inches_to_ft <- function(x){
x/12
}
inches_to_ft(144)
ifelse(inches_to_ft(heights$height) < 5, 1, 0)
x <- ifelse(inches_to_ft(heights$height) < 5, 1, 0)
sum[x{0}]
sum[x[1]]
sum(inches_to_ft(heights$height) < 5)
# define a vector of length m
m <- 10
f_n <- vector(length = m)
# make a vector of factorials
_________{
m <- 10
f_n <- vector(length = m)
for(n in 1:m){
f_n[n] <- factorial(n)
}
f_n
